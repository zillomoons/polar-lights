let initialState = {
    friends: [
        {id: 1, name: 'Zein'},
        {id: 2, name: 'Mia'},
        {id: 3, name: 'HoneyBee'}
        
    ]
}
const sidebarReducer = (state = initialState, action) => {
    return state;
}

export default sidebarReducer;